import styled from 'styled-components';
import { QUERIES } from '../../../styling/styles';

export const ContainerDiscover = styled.div`
  /* border: 3px solid burlywood; */
  overflow-y: auto;
  height: 57rem;
  padding-top: 2rem;

  /* @media (${QUERIES.medium}) {
    width: 80%;
    align-self: flex-start;
    justify-self: center;
  }

  @media (${QUERIES.small}) {
    align-self: flex-start;
    justify-self: center;
    width: 100%;
    height: 3rem;
  } */
`;
